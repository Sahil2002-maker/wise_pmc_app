import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryGreen = Color(0xFF198754);
  static const Color primaryGreenDark = Color(0xFF157347);

  static const Color textDark = Color(0xFF363636);
  static const Color textMutedDark = Color(0xFF4B4B4B);
  static const Color secondaryText = Color(0xFF6E6B7B);

  static const Color borderColor = Color(0xFFD8D6DE);
  static const Color hintColor = Color(0xFFB9B9C3);

  static const Color bgColor = Color(0xFFF8F8F8);
  static const Color lightBg = Color(0xFFF6F6F6);
  static const Color white = Colors.white;

  static const Color requiredRed = Color(0xFFEA5455);

  static const Color completed = Color(0xFF28C76F);
  static const Color assigned = Color(0xFF00CFE8);
  static const Color pending = Color(0xFFFF9F43);

  static const Color sidebarActiveStart = Color(0xFF198754);
  static const Color sidebarActiveEnd = Color(0xFF41B883);

    // ── Brand Colors (extracted from Wise Realty screenshots) ─────────────────
  static const Color primary      = Color(0xFF2E7D32); // Deep green
  static const Color primaryLight = Color(0xFF4CAF50); // Light green
  static const Color primaryDark  = Color(0xFF1B5E20); // Dark green
  static const Color accent       = Color(0xFFC8A882); // Warm tan/beige

  // ── Sidebar Colors ────────────────────────────────────────────────────────
  static const Color sidebarBg          = Color(0xFFFFFFFF); // White sidebar
  static const Color sidebarHeader      = Color(0xFF2E7D32); // Green header
  static const Color sidebarActive      = Color(0xFF2E7D32); // Active item bg
  static const Color sidebarActiveText  = Color(0xFF2E7D32); // Active item text
  static const Color sidebarInactiveText= Color(0xFF6B7280); // Inactive text
  static const Color sidebarHover       = Color(0xFFE8F5E9); // Hover state
  static const Color sidebarDivider     = Color(0xFFF0F2F5); // Divider color
  static const Color sidebarIconActive  = Color(0xFF2E7D32);
  static const Color sidebarIconInactive= Color(0xFF9CA3AF);

  // ── Dashboard Card Colors ─────────────────────────────────────────────────
  static const Color cardBlue    = Color(0xFF3B82F6);
  static const Color cardGreen   = Color(0xFF22C55E);
  static const Color cardOrange  = Color(0xFFF59E0B);
  static const Color cardRed     = Color(0xFFEF4444);
  static const Color cardPurple  = Color(0xFF8B5CF6);
  static const Color cardCyan    = Color(0xFF06B6D4);

  // ── Leave Type Colors (matching screenshot) ───────────────────────────────
  static const Color leaveGreen  = Color(0xFF22C55E); // Casual
  static const Color leaveOrange = Color(0xFFF59E0B); // Sick
  static const Color leaveBlue   = Color(0xFF3B82F6); // Privilege
  static const Color leaveGray   = Color(0xFF6B7280); // Comp-off
  static const Color leaveRed    = Color(0xFFEF4444); // Loss of pay

  // ── Background Colors ─────────────────────────────────────────────────────
  static const Color background  = Color(0xFFF0F2F5);
  static const Color surface     = Color(0xFFFFFFFF);
  static const Color cardBg      = Color(0xFFFFFFFF);

  // ── Text Colors ───────────────────────────────────────────────────────────
  static const Color textPrimary   = Color(0xFF2D2D2D);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textHint      = Color(0xFF9CA3AF);
  static const Color textLink      = Color(0xFF2E7D32);

  // ── Border & Input Colors ─────────────────────────────────────────────────
  static const Color border      = Color(0xFFE5E7EB);
  static const Color borderFocus = Color(0xFF2E7D32);
  static const Color inputBg     = Color(0xFFFFFFFF);

  // ── Status Colors ─────────────────────────────────────────────────────────
  static const Color error        = Color(0xFFDC2626);
  static const Color errorLight   = Color(0xFFFEE2E2);
  static const Color success      = Color(0xFF16A34A);
  static const Color successLight = Color(0xFFDCFCE7);
  static const Color warning      = Color(0xFFD97706);
  static const Color warningLight = Color(0xFFFEF3C7);
  static const Color info         = Color(0xFF0EA5E9);
  static const Color infoLight    = Color(0xFFE0F2FE);

  // ── Shadow ────────────────────────────────────────────────────────────────
  static const Color shadow       = Color(0x14000000);
  static const Color shadowMedium = Color(0x1A000000);

  // ── Decorative ────────────────────────────────────────────────────────────
  static const Color decorCard1 = Color(0xFFDDE1E7);
  static const Color decorCard2 = Color(0xFFE8EBF0);
}